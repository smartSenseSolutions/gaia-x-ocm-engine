import type * as Indy from 'indy-sdk';
import { AgentConfig } from '../../../agent/AgentConfig';
export declare class IndyVerifierService {
    private indy;
    constructor(agentConfig: AgentConfig);
    verifyProof({ proofRequest, proof, schemas, credentialDefinitions, revocationRegistryDefinitions, revocationStates, }: VerifyProofOptions): Promise<boolean>;
}
export interface VerifyProofOptions {
    proofRequest: Indy.IndyProofRequest;
    proof: Indy.IndyProof;
    schemas: Indy.Schemas;
    credentialDefinitions: Indy.CredentialDefs;
    revocationRegistryDefinitions?: Indy.RevRegsDefs;
    revocationStates?: Indy.RevStates;
}

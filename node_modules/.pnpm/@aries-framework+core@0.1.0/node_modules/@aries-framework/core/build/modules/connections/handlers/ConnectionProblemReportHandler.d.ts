import type { Handler, HandlerInboundMessage } from '../../../agent/Handler';
import type { ConnectionService } from '../services';
import { ConnectionProblemReportMessage } from '../messages';
export declare class ConnectionProblemReportHandler implements Handler {
    private connectionService;
    supportedMessages: (typeof ConnectionProblemReportMessage)[];
    constructor(connectionService: ConnectionService);
    handle(messageContext: HandlerInboundMessage<ConnectionProblemReportHandler>): Promise<void>;
}

import type { Handler, HandlerInboundMessage } from '../../../agent/Handler';
import type { CredentialService } from '../services';
import { CredentialProblemReportMessage } from '../messages';
export declare class CredentialProblemReportHandler implements Handler {
    private credentialService;
    supportedMessages: (typeof CredentialProblemReportMessage)[];
    constructor(credentialService: CredentialService);
    handle(messageContext: HandlerInboundMessage<CredentialProblemReportHandler>): Promise<void>;
}

import type { Handler, HandlerInboundMessage } from '../../../agent/Handler';
import { DiscloseMessage } from '../messages';
export declare class DiscloseMessageHandler implements Handler {
    supportedMessages: (typeof DiscloseMessage)[];
    handle(inboundMessage: HandlerInboundMessage<DiscloseMessageHandler>): Promise<void>;
}

import type { Handler, HandlerInboundMessage } from '../../../agent/Handler';
import type { ProofService } from '../services';
import { PresentationAckMessage } from '../messages';
export declare class PresentationAckHandler implements Handler {
    private proofService;
    supportedMessages: (typeof PresentationAckMessage)[];
    constructor(proofService: ProofService);
    handle(messageContext: HandlerInboundMessage<PresentationAckHandler>): Promise<void>;
}

import type { Handler, HandlerInboundMessage } from '../../../agent/Handler';
import type { DiscoverFeaturesService } from '../services/DiscoverFeaturesService';
import { QueryMessage } from '../messages';
export declare class QueryMessageHandler implements Handler {
    private discoverFeaturesService;
    supportedMessages: (typeof QueryMessage)[];
    constructor(discoverFeaturesService: DiscoverFeaturesService);
    handle(inboundMessage: HandlerInboundMessage<QueryMessageHandler>): Promise<import("../../../types").OutboundMessage<import("../messages").DiscloseMessage>>;
}

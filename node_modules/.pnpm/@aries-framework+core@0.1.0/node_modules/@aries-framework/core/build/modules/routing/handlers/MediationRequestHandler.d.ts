import type { AgentConfig } from '../../../agent/AgentConfig';
import type { Handler, HandlerInboundMessage } from '../../../agent/Handler';
import type { MediatorService } from '../services/MediatorService';
import { MediationRequestMessage } from '../messages/MediationRequestMessage';
export declare class MediationRequestHandler implements Handler {
    private mediatorService;
    private agentConfig;
    supportedMessages: (typeof MediationRequestMessage)[];
    constructor(mediatorService: MediatorService, agentConfig: AgentConfig);
    handle(messageContext: HandlerInboundMessage<MediationRequestHandler>): Promise<import("../../../types").OutboundMessage<import("..").MediationGrantMessage> | undefined>;
}

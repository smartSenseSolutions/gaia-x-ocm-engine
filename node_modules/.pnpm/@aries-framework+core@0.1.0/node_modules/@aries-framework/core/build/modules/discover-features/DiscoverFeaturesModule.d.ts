import { Dispatcher } from '../../agent/Dispatcher';
import { MessageSender } from '../../agent/MessageSender';
import { ConnectionService } from '../connections/services';
import { DiscoverFeaturesService } from './services';
export declare class DiscoverFeaturesModule {
    private connectionService;
    private messageSender;
    private discoverFeaturesService;
    constructor(dispatcher: Dispatcher, connectionService: ConnectionService, messageSender: MessageSender, discoverFeaturesService: DiscoverFeaturesService);
    queryFeatures(connectionId: string, options: {
        query: string;
        comment?: string;
    }): Promise<void>;
    private registerHandlers;
}

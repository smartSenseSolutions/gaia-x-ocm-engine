import type { Handler, HandlerInboundMessage } from '../../../agent/Handler';
import type { ProofService } from '../services';
import { PresentationProblemReportMessage } from '../messages';
export declare class PresentationProblemReportHandler implements Handler {
    private proofService;
    supportedMessages: (typeof PresentationProblemReportMessage)[];
    constructor(proofService: ProofService);
    handle(messageContext: HandlerInboundMessage<PresentationProblemReportHandler>): Promise<void>;
}

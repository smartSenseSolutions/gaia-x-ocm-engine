import type { ServerConfig } from '../utils/ServerConfig';
import type { Agent } from '@aries-framework/core';
export declare const basicMessageEvents: (agent: Agent, config: ServerConfig) => Promise<void>;

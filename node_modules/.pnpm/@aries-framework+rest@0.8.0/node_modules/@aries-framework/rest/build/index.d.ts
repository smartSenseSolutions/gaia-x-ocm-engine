import 'reflect-metadata';
import type { ServerConfig } from './utils/ServerConfig';
import type { Agent } from '@aries-framework/core';
export declare const startServer: (agent: Agent, config: ServerConfig) => Promise<void>;

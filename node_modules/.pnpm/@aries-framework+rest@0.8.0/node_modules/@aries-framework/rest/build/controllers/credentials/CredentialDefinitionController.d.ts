import { Agent } from '@aries-framework/core';
import { CredentialDefinitionRequest } from '../../schemas/CredentialDefinitionRequest';
export declare class CredentialDefinitionController {
    private agent;
    constructor(agent: Agent);
    /**
     * Retrieve credentialDefinition by credentialDefinitionId
     */
    getCredentialDefinitionById(credentialDefinitionId: string): Promise<import("indy-sdk").CredDef>;
    /**
     * Creates a new CredentialDefinition.
     * Returns CredentialDefinitionId and CredentialDefinition
     */
    createCredentialDefinition(credentialDefinitionRequest: CredentialDefinitionRequest): Promise<import("indy-sdk").CredDef>;
}

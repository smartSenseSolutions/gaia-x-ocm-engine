import { Agent } from '@aries-framework/core';
import { BasicMessageRequest } from '../../schemas/BasicMessageRequest';
export declare class BasicMessageController {
    private agent;
    constructor(agent: Agent);
    /**
     * Retrieve basic messages by connectionId
     */
    getBasicMessages(connectionId: string): Promise<Record<string, unknown>[]>;
    /**
     * Send a basic message to a connection
     */
    sendMessage(connectionId: string, basicMessage: BasicMessageRequest): Promise<void>;
}

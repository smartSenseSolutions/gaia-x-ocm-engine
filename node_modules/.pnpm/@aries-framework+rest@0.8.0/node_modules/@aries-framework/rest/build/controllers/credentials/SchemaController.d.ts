import { Agent } from '@aries-framework/core';
import { SchemaTemplate } from '../../schemas/SchemaRequest';
export declare class SchemaController {
    private agent;
    constructor(agent: Agent);
    /**
     * Retrieve schema by schemaId
     */
    getSchemaById(schemaId: string): Promise<import("indy-sdk").Schema>;
    /**
     * Creates a new schema and registers schema on ledger
     * Returns created schema
     */
    createSchema(schema: SchemaTemplate): Promise<import("indy-sdk").Schema>;
}
